Sec Grp = sg-00ba4c7def9e8e341

Created a new key

aws ec2 create-key-pair --key-name michaelkeypair --query 'KeyMaterial' --output text > michaelkeypair.pem


ami-0ff760d16d9497662 image for the Ireland region:


aws ec2 run-instances --image-id ami-0ff760d16d9497662 --count 1 --instance-type t2.medium --key-name michaelkeypair --security-group-ids sg-00ba4c7def9e8e341

ec2-52-215-235-133.eu-west-1.compute.amazonaws.com

aws ec2 terminate-instances --instance-ids "i-07937c9a2c367ee2e"

Instance ID: "i-018b72c7cfdd2ad39"

ssh -i michaelkeypair.pem centos@ec2-52-215-235-133.eu-west-1.compute.amazonaws.com


sudo yum update -y 

sudo yum install git maven -y

#Verify that the two are installed
java -version
python --version

sudo yum install epel-release

sudo yum update -y 
sudo yum install pip -y
sudo yum install python-pip -y
sudo yum install awscli -y

#verify AWSCLI installed correctly.
aws --version

git clone https://github.com/dev-minds/dm_training_2019.git

cd dm_training_2019.git

git checkout develop

cd dm_ci/java/shopizer/

sudo mvn clean install

mvn spring-boot:run

#back to the local session:
aws ec2 authorize-security-group-ingress \
    --group-id sg-00ba4c7def9e8e341 \
    --protocol tcp \
    --port 8080 \
    --cidr 0.0.0.0/0





